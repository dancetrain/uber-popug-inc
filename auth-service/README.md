# OAuth Server

```
tokenEndpoint = "/oauth/token"
authorizationEndpoint = "/oauth/authorize"
tokenInfoEndpoint = "/oauth/tokeninfo"
```

## Data model

Auth server has many applications with its own users with multiple users provider provides different access tokens.
